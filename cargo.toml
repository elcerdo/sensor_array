[package]
name = "sensor_array"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = "0.15"
rand = "0.8"
rand_distr = "0.4"
bevy_egui = "0.31"

# Optional: faster compile times during development
# Uncomment the lines below and run: cargo run
# [profile.dev]
# opt-level = 1

# [profile.dev.package."*"]
# opt-level = 3